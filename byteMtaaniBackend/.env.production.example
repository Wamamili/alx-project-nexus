# Production environment example — copy to `.env.production` and set values securely

# Django
DEBUG=False
SECRET_KEY=replace-with-a-secure-random-32+ char string
DJANGO_ENV=production
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database (Postgres)
DATABASE_URL=postgres://prod_user:strong_password@db:5432/production_db

# Redis (cache/session)
REDIS_URL=redis://:strong_redis_password@redis:6379/1

# Gunicorn
GUNICORN_WORKERS=4

# NOTE: Never commit real secrets to source control. Use your host's secret manager or CI/CD secrets.
